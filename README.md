# homebrew-tap

Homebrew tap for [KScratch](https://github.com/kscratch-app) — a Kubernetes desktop app.

## Install

```bash
brew tap kscratch-app/tap
brew install --cask kscratch
```

## Available casks

| Cask | Description |
|------|-------------|
| `kscratch` | Kubernetes desktop app for visualizing and managing clusters |
